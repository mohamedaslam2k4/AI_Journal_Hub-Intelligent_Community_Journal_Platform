export function JournalCard({ journal }) { return <article className="card space-y-3"><div className="flex items-start justify-between gap-4"><h3 className="text-xl font-bold">{journal.title}</h3><span className="rounded-full bg-indigo-100 px-3 py-1 text-sm text-indigo-700">{journal.category || 'Other'}</span></div><p className="text-slate-600 dark:text-slate-300">{journal.summary || journal.content}</p><div className="flex flex-wrap gap-2">{(journal.tags || []).map((tag) => <span key={tag} className="rounded-full bg-slate-100 px-2 py-1 text-xs dark:bg-slate-800">#{tag}</span>)}</div><p className="text-sm text-slate-500">By {journal.author_name} · {new Date(journal.created_at).toLocaleDateString()}</p></article>; }
